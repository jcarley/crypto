package main

import (
	"testing"
	// . "github.com/smartystreets/goconvey/convey"
)

func TestEncryptingPasswords(t *testing.T) {
	// Convey("EncryptPassword returns a hashed string", t, func() {
	//   clearPassword := "ThisIsSecret"
	//   hashedPassword, err := EncryptPassword(clearPassword)
	//
	//   So(err, ShouldBeNil)
	//   So(clearPassword, ShouldNotEqual, hashedPassword)
	//   So(clearPassword, ShouldHaveSameTypeAs, hashedPassword)
	//   So(hashedPassword, ShouldNotBeBlank)
	// })
	//
	// Convey("VerifyPassword", t, func() {
	//   Convey("When passwords match returns nil", func() {
	//     clearPassword := "ThisIsSecret"
	//     hashedPassword, _ := EncryptPassword(clearPassword)
	//     err := VerifyPassword(hashedPassword, clearPassword)
	//
	//     So(err, ShouldBeNil)
	//   })
	//
	//   Convey("When passwords don't match returns an error", func() {
	//     clearPassword := "ThisIsSecret"
	//     wrongPassword := "ThisIsWrong"
	//     hashedPassword, _ := EncryptPassword(clearPassword)
	//     err := VerifyPassword(hashedPassword, wrongPassword)
	//
	//     So(err, ShouldNotBeNil)
	//   })
	// })
}
